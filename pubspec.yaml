name: perfume_erp
description: نظام محاسبة ومخزون للعطور والساعات والتجميل
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path_provider: ^2.0.11
  provider: ^6.0.5
  intl: ^0.18.1
  flutter_localizations:
    sdk: flutter
  url_launcher: ^6.1.10
  share_plus: ^6.3.0
  pdf: ^3.10.4
  printing: ^5.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/screenshots/
